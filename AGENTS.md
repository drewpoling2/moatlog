# Agent Behavioral Skills
Generated by moatlog — see moatlog.dev/docs

<!-- moatlog-skills-start -->
## Behavioral Skills

<!-- moatlog-llm-generated -->

> Auto-generated by moatlog from .moatlog/moat.json
> Regenerate: `moatlog skills generate`

### docs-landing-visual
Update landing page components and styles for visual changes

**When this applies:** When editing docs/app/components/landing/LandingPage.tsx or docs/styles/marketing/landing.css

**Core files:**
- `docs/app/components/landing/LandingPage.tsx` (100%)
- `docs/styles/marketing/landing.css` (100%)

**Before starting:** LandingPage.tsx defines the structure and interactivity of landing components, while landing.css provides visual styling. These files change together because layout decisions (like making cards clickable) require coordinated updates to both component logic and visual presentation. Edit the component first to add interactivity, then update the stylesheet to ensure the visual changes render correctly.

---

### docs-tokens-styling
Update design tokens and typography configuration

**When this applies:** When editing docs/app/globals.css or docs/app/layout.tsx

**Core files:**
- `docs/app/globals.css` (100%)
- `docs/app/layout.tsx` (100%)

**Before starting:** globals.css holds design tokens (colors, fonts, spacing) that layout.tsx applies across the application. Token changes require updating the stylesheet first, then verifying that layout.tsx correctly uses the new tokens. Watch for typography and color inconsistencies when modifying token definitions.

---

### core-distiller-attribution
Work with event counting, read/write attribution, and behavioral analysis

**When this applies:** When editing packages/core/src/task-context.ts, packages/core/src/types.ts, packages/core/src/distiller.ts, or packages/mcp/src/server.ts

**Core files:**
- `packages/core/src/distiller.ts` (100%)
- `packages/core/src/task-context.ts` (100%)
- `packages/core/src/types.ts` (100%)
- `packages/mcp/src/server.ts` (100%)

**Before starting:** The distiller analyzes task context and event data to compute metrics like read/write attribution and total event counts. task-context.ts and types.ts define the data structures, while distiller.ts implements the analysis logic, and server.ts logs events. Changes to event definitions in types.ts usually require corresponding logic updates in distiller.ts and possibly task-context.ts.

---

### project-skills-generation
Implement skill generation from behavioral patterns using LLM

**When this applies:** When working on moatlog skills generation in packages/core/src/skills.ts, packages/cli/src/commands/skills.ts, or related LLM cascade infrastructure

**Core files:**
- `packages/core/src/skills.ts` (100%)
- `packages/core/src/index.ts` (67%)
- `packages/core/src/llm.ts` (67%)
- `packages/cli/src/bin.ts` (67%)
- `packages/cli/src/commands/merge.ts` (67%)
- `packages/cli/src/commands/skills.ts` (67%)

**Before starting:** Skills generation parallels the merge command's LLM cascade pattern. core/skills.ts contains the generation logic, while cli/commands/skills.ts wires it into the CLI. Start by reviewing packages/cli/src/commands/merge.ts to understand the LLM cascade pattern, then apply it to skills generation. Pay attention to prompt engineering and response parsing.

---
<!-- moatlog-skills-end -->
