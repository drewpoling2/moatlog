---
description: Update design tokens and typography configuration
globs:
alwaysApply: false
---

# moatlog: docs-tokens-styling

<!-- moatlog-llm-generated -->

> Auto-generated from .moatlog/moat.json — do not edit.
> Regenerate: `moatlog skills generate`

## When this applies
When editing docs/app/globals.css or docs/app/layout.tsx

## Core files
- `docs/app/globals.css`
  (100% of similar tasks)
- `docs/app/layout.tsx`
  (100% of similar tasks)

## Co-access patterns
- `docs/app/globals.css` ↔ `docs/app/layout.tsx` (support: 4)

## Example tasks
- "please update my typography to be DM mono for mono font and for sans helvetica, arial, system-ui"
- "Hex RGB #9bbc0f (155,188,15) #8bac0f (139,172,15) #306230 (48,98,48) #0f380f (15,56,15) are these the colors used for this?"

## Before starting
globals.css holds design tokens (colors, fonts, spacing) that layout.tsx applies across the application. Token changes require updating the stylesheet first, then verifying that layout.tsx correctly uses the new tokens. Watch for typography and color inconsistencies when modifying token definitions.

---
*Skill generated by moatlog from behavioral data.
Keep current: `moatlog skills generate` after significant
work in this area.*
